# Spec-Driven Development Constitution

This repository follows a light Spec-Driven Development approach. Feature behavior is captured in human-readable specs stored under the `specs/` directory. Each spec should describe the feature intent, scenarios, and expected outcomes.

Guidelines
- Keep specs concise and example-driven.
- Reference templates in `.specify/templates/` when creating new specs.
- Keep implementation and specs synchronized; update specs when behavior changes.
